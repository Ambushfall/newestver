services:
    app:
        container_name: pybot-app
        # build: ./
        image: docker.io/lukacolic/ytdiscord-pybot:latest
        volumes:
            - :/usr/src/app/dl
            - :/usr/src/app/cfg